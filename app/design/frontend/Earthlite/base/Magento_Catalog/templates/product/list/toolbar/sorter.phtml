<?php
/**
 * Copyright © Magento, Inc. All rights reserved.
 * See COPYING.txt for license details.
 */
?>
<?php
/**
 * Product list toolbar
 *
 * @var $block \Magento\Catalog\Block\Product\ProductList\Toolbar
 */

 $directionParam = "";
 if ($block->getCurrentDirection() == 'desc') {
     $directionParam = '&product_list_dir=desc';
 }
?>
<div class="toolbar-sorter sorter">
    <label class="sorter-label" for="sorter"><?= $block->escapeHtml(__('Sort By')) ?></label>

    <select id="sorter" data-role="sorter" class="sorter-options d-md-inline-block">
        <?php foreach ($block->getAvailableOrders() as $_key => $_order) :?>
            <option value="<?= $block->escapeHtmlAttr($_key) ?>"
                <?php if ($block->isOrderCurrent($_key)) :?>
                    selected="selected"
                <?php endif; ?>
                >
                <?= $block->escapeHtml(__($_order)) ?>
            </option>
        <?php endforeach; ?>
    </select>
    <ul data-role="sorter" class="sorter-select d-md-none">
        <?php foreach ($block->getAvailableOrders() as $_key => $_order): ?>
            <li><a href="<?= $block->getPagerUrl("product_list_order=" . $_key . $directionParam) ?>" 
            <?php if ($block->isOrderCurrent($_key)): ?> 
                class="active"
            <?php endif; ?> 
            >
            <?= $block->escapeHtml(__($_order)) ?>
            </a></li>   
        <?php endforeach; ?>
    </ul>

    <?php if ($block->getCurrentDirection() == 'desc') :?>
        <a title="<?= $block->escapeHtmlAttr(__('Set Ascending Direction')) ?>"
           href="<?= $block->getPagerUrl() ?>"
           class="action sorter-action sort-desc"
           data-role="direction-switcher"
           data-value="asc">
            <span><?= $block->escapeHtml(__('Set Ascending Direction')) ?></span>
        </a>
    <?php else :?>
        <a title="<?= $block->escapeHtmlAttr(__('Set Descending Direction')) ?>"
           href="product_list_dir=desc"
           class="action sorter-action sort-asc"
           data-role="direction-switcher"
           data-value="desc">
            <span><?= $block->escapeHtml(__('Set Descending Direction')) ?></span>
        </a>
    <?php endif; ?>
</div>

<script type="text/x-magento-init">
{
    "*": {
        "Magento_Catalog/js/sorter":{}
    }
}
</script>
