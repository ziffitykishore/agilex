{
  "dependencies": {
    "gulp": "^3.9.1"
  },
  "engines": {
    "node": ">=8.9.0 <11.0.0"
  },
  "scripts": {
    "build": "gulp build",
    "build:production": "gulp build --prod --ci",
    "start": "gulp watch"
  }
}
